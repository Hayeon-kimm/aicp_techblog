theme: jekyll-theme-chirpy

lang: ko

timezone: Asia/Seoul

title: "UNIST AICP Team '아자잣'"
tagline: "Agent기반 자동화 블로그"

description: >-
  PPT Auto-generation과 AI Agent를 연구합니다. Agent기반 자동화 블로그

url: "https://hayeon-kimm.github.io"
baseurl: "/aicp_techblog"

github:
  username: Hayeon-kimm

social:
  name: Hayeon Kim
  links:
    - https://github.com/Hayeon-kimm

# Web Analytics
analytics:
  google:
    id: ""

# Preferences
theme_mode: dark

avatar: ""

toc: true

comments:
  provider: ""

assets:
  self_host:
    enabled: false

paginate: 10

kramdown:
  input: GFM
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: highlight
    block:
      line_numbers: true

collections:
  tabs:
    output: true
    sort_by: order

defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post
      comments: false
      toc: true
      permalink: /posts/:title/
  - scope:
      path: _tabs
      type: tabs
    values:
      layout: page
      permalink: /:title/

sass:
  style: compressed

exclude:
  - "*.gem"
  - "*.gemspec"
  - docs
  - tools
  - README.md
  - LICENSE
  - "*.config.js"
  - package*.json

jekyll-archives:
  enabled: [categories, tags]
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/

plugins:
  - jekyll-feed
  - jekyll-archives
  - jekyll-sitemap
  - jekyll-seo-tag
